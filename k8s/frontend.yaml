apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: sofiene-chouchene.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: 4.207.208.120
                port:
                  number: 80
